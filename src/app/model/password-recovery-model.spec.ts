import { PasswordRecoveryModel } from './password-recovery-model';

describe('PasswordRecoveryModel', () => {
  it('should create an instance', () => {
    expect(new PasswordRecoveryModel()).toBeTruthy();
  });
});
